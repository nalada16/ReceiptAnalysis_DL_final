# 機器學習期末專案企劃書

## 電子發票明細語意分類與個人消費行為診斷

本專案以台灣電子發票明細作為資料來源，目標不是單純建立記帳圖表，而是建立一個能理解發票品項、預測短期消費趨勢，並進一步偵測異常消費與拆解消費增量原因的機器學習系統。整體系統分為兩個主要任務：第一個任務處理發票明細的語意分類，第二個任務在分類結果之上進行個人消費行為診斷。

為了避免期末專案範圍過大，本專案將任務分為「核心任務」與「延伸任務」。核心任務是發票分類與短期消費金額預測；延伸任務則是異常消費偵測與個人通膨拆解。若資料量或時間不足，會優先確保核心任務完整訓練、評估與分析，再完成延伸任務的實驗版或案例分析版。

---

## 一、研究動機與問題定義

現有記帳 App 大多停留在回顧式統計，例如顯示使用者這個月在飲食、交通、購物等類別各花了多少錢。然而，這類工具通常無法回答更深入的問題，例如：

- 發票品名很模糊時，是否能透過店家名稱判斷消費類別？
- 使用者接下來一週的消費金額可能如何變化？
- 哪些消費日明顯偏離平常習慣，可能屬於異常或衝動型消費？
- 本月支出增加，是因為買更多，還是常買品項變貴？

本專案的核心差異在於把電子發票從「消費紀錄」轉換成「可分析的行為資料」。第一階段使用 NLP 模型理解品名與店家語意，第二階段把分類後的每日消費資料轉成時序資料，進行預測與診斷。

---

## 二、整體系統架構

整體流程如下：

1. 原始電子發票 CSV 匯入
2. 資料去識別化與欄位清洗
3. 發票品項文字正規化
4. LLM 輔助初始標註與人工審核
5. Task 1：BERT 發票明細語意分類
6. 每筆明細附上消費類別與語意向量
7. 依日期彙整成每日消費時序資料
8. Task 2 Layer 1：短期消費金額預測
9. Task 2 Layer 2：異常消費偵測
10. Task 2 Layer 3：個人通膨與單價變化拆解

系統中最重要的設計是 Task 1 的 BERT 模型不只輸出分類結果，也保留中間層語意向量。這些向量可以被後續任務使用，例如作為時序模型的額外特徵，或用於 BERTopic 與 HDBSCAN 將相似商品自動分群。

---

## 三、資料來源與前處理

### 3.1 資料來源

資料來自財政部電子發票整合服務平台匯出的個人電子發票 CSV，由組員與親友自願提供。原始欄位預期包含發票日期、賣方名稱、賣方地址、品名、數量、單價與金額。

為了降低隱私風險，本專案只保留與模型訓練必要相關的欄位：

- 保留：日期、賣方名稱、品名、數量、單價、金額
- 移除：手機載具條碼、買方統編、個人姓名、完整地址
- 地址處理：若需要地區資訊，只保留縣市或行政區，不保留門牌

### 3.2 資料規模目標

| 資料類型 | 目標規模 | 用途 |
|---|---:|---|
| 原始發票明細 | 2,000 筆以上 | 全部任務基礎 |
| 已標註分類資料 | 1,500 筆以上 | Task 1 分類模型 |
| 個人每日消費時序 | 3 至 5 人，每人 3 至 6 個月 | Task 2 預測與異常偵測 |
| 重複購買品項 | 每個品項群至少 5 至 10 次 | 通膨拆解與單價追蹤 |

考量互評中提到資料量可能不足，本專案會把模型設計控制在資料量可支撐的範圍內。Task 1 會使用 pre-trained BERT fine-tuning，而不是從零訓練語言模型；Task 2 會同時建立傳統 baseline，避免只呈現深度模型但無法判斷是否真的有效。

### 3.3 文字前處理

品名與店家名稱的前處理包含：

- 去除前後空白與多餘符號
- 全形轉半形
- 英文字母統一大小寫
- 數字與規格保留，例如「600ml」、「2入」、「L」
- 店家名稱正規化，例如移除「股份有限公司」、「分公司」等非核心字，但保留品牌與地區資訊
- 對缺漏品名或缺漏店家建立特殊 token，例如 `[UNK_ITEM]`、`[UNK_STORE]`

全形轉半形會使用 Unicode code point 轉換處理。常見全形字元位於 `U+FF01` 到 `U+FF5E`，可轉成對應半形字元；全形空白 `U+3000` 則轉為一般空白。

### 3.4 每日消費時序前處理

每日消費資料會依日期與類別彙整為矩陣：

`X_t = [飲食, 交通, 購物, 3C, 娛樂, 醫療健康, 教育, 其他]`

對於沒有消費紀錄的日期，不能直接視為遺失資料，而是代表「當天沒有發票紀錄」。因此本專案會同時保留兩種資訊：

- 金額欄位補 0，代表該日該類別沒有紀錄到消費
- 額外加入 `has_transaction` mask，標記該日是否有任何發票紀錄

這樣可以降低單純補 0 對平均值與模型訓練造成的偏誤。評估時也會分開觀察「有消費日」與「全日期」的預測表現。

---

## 四、Task 1：發票明細語意分類

### 4.1 任務定義

輸入為單筆發票明細：

`品名 [SEP] 店家名稱 [SEP] 金額區間`

輸出為消費類別：

1. 飲食
2. 交通
3. 購物
4. 3C 電子
5. 娛樂
6. 醫療健康
7. 教育
8. 其他

在初版企劃中，輸入主要使用「品名 + 店家」。根據互評建議，新版會額外考慮金額區間，例如低金額飲料、較高金額 3C 或醫療支出，可能有助於分類。但金額不會作為唯一判斷依據，避免模型只學到價格偏差。

### 4.2 為什麼需要 BERT

電子發票品名常出現語意不足的情況，例如「餐飲」、「小吃」、「商品」、「Food Court」。只看品名時，模型無法知道實際消費情境；但若加入店家名稱，例如百貨、便利商店、交通場站或藥妝店，就能提供重要上下文。

本專案使用 BERT 的原因是它能同時處理品名與店家名稱之間的語意關係。模型輸入格式為：

`[CLS] 品名 [SEP] 店家名稱 [SEP] 金額區間 [SEP]`

使用 `[CLS]` token 的輸出接上 linear classification head，預測 8 類消費標籤。

### 4.3 模型選擇

主要模型：

- `ckiplab/bert-base-chinese`
- 原因：支援繁體中文，較適合台灣商家名稱與中文品名

對照模型：

- TF-IDF + Logistic Regression
- TF-IDF + MLP
- Regex 關鍵字規則
- BERT 但只輸入品名，不輸入店家名稱

這些 baseline 可以回答兩個問題：

- BERT 是否真的比傳統文字分類方法有效？
- 加入店家名稱是否真的能改善空洞品名分類？

### 4.4 標註策略

完全人工標註成本高，完全依賴 LLM 又可能有錯誤。因此採用半自動標註：

1. 先以規則篩掉明顯樣本，例如含有「悠遊卡」、「停車費」、「維他命」等關鍵字的資料。
2. 對模糊樣本使用 LLM 批次預標註。
3. 組員人工審核至少 20% 的樣本。
4. 對模型信心低、品名空洞、LLM 與規則不一致的資料提高人工審核比例。
5. 建立標註準則文件，減少不同組員標註不一致。

為了控制 LLM 成本，不會對每筆資料都直接送 LLM。優先用規則與去重後的「品名 + 店家」組合降低 token 使用量。例如同一組「珍珠奶茶 + 某飲料店」只需標註一次，再回填到重複紀錄。

### 4.5 Multi-label 問題處理

互評中提到八種分類可能有重疊，例如停車費可能同時和交通、娛樂出遊有關。本專案先採用 single-label 分類，因為發票明細本身通常只能提供品項與店家，若直接 multi-label 可能標註標準不穩。

但會保留兩個輔助設計：

- 主類別：模型訓練與評估使用，例如交通
- 情境備註：人工分析時可記錄，例如出遊相關

若後續資料量足夠，可把 multi-label 作為延伸研究。

### 4.6 評估指標

主要指標：

- Macro F1：避免大類別如飲食主導結果
- Weighted F1：反映整體資料分布下的表現
- Confusion Matrix：分析哪些類別容易混淆

輔助指標：

- Accuracy：作為直觀參考，但不作為唯一指標
- 空洞品名 subset F1：只針對品名模糊的資料評估，驗證加入店家名稱是否有效

---

## 五、Task 2：消費行為診斷

Task 2 分成三層。Layer 1 是核心任務，Layer 2 與 Layer 3 是延伸任務。

### 5.1 Layer 1：短期消費金額預測

#### 任務定義

輸入：

- 過去 30 天每日各類別消費金額
- 時間特徵，例如星期幾、是否週末、當月第幾週、距月底天數
- Task 1 產生的語意輔助特徵，例如每日消費明細 embedding 平均

輸出：

- 未來 7 天每日總消費金額
- 可選：未來 7 天每日各類別消費金額

#### 模型設計

主要模型：

- LSTM
- 輸入為每日消費向量與時間特徵
- 2 層 LSTM，hidden size 初始設為 64 或 128
- 輸出未來 7 天金額

對照模型：

- Moving Average：以前 7 天平均預測未來 7 天
- ARIMA：傳統時序 baseline
- MLP：將 30 天 window flatten 後預測
- LSTM without semantic features：檢查語意向量是否有幫助
- Transformer Encoder：若資料量允許，作為進階比較

考量資料量有限，LSTM 會作為主要深度模型，Transformer 只作為延伸比較。若 Transformer 表現不穩，會在報告中如實分析原因，而不強行把它作為主要模型。

#### 評估指標

- MAE：主要指標，容易解釋成平均差幾元
- RMSE：觀察高金額日期的誤差
- MAPE 或 sMAPE：比較不同使用者消費規模
- Baseline improvement：相對移動平均是否有改善

資料切分必須依時間順序進行，不能隨機切分，避免未來資料洩漏到訓練集。

### 5.2 Layer 2：異常消費偵測

#### 名稱修正

初版使用「情緒消費偵測」，但互評指出情緒本身無法從發票直接觀察。因此新版將正式任務名稱改為「異常消費偵測」，並把「可能的情緒或衝動消費」作為解釋層，而不是直接聲稱模型能判斷情緒。

也就是說，模型偵測的是：

- 消費金額明顯高於個人平常模式
- 消費類別組合與平常不同
- 發生時間具有特定規律，例如週五晚上、月底前、考試週後

模型不直接判斷使用者當下情緒。

#### 技術方法

主要方法：

- LSTM Autoencoder

模型會學習使用者正常的消費序列模式。輸入為一段連續消費資料，模型嘗試重建原始序列。若某天或某段時間的 reconstruction error 明顯偏高，代表該消費模式和過去習慣不同，可標記為異常。

異常分數：

`anomaly_score = reconstruction_error`

判斷方式：

- 使用 validation set 的 reconstruction error 分布設定 threshold
- 例如取第 95 百分位作為異常門檻
- 不同使用者分開建 threshold，避免高消費者與低消費者混在一起比較

#### 解釋方式

偵測到異常後，系統會分析造成異常的主要因素：

- 哪一類別金額增加最多
- 是否出現在特定時間特徵，例如週末、月底、假日
- 是否由高價商品造成
- 是否屬於少見類別，例如平常很少買 3C，但某天突然出現高額 3C 消費

這樣可以避免只輸出「異常」兩個字，而是讓診斷更有解釋性。

### 5.3 Layer 3：個人通膨與單價變化拆解

#### 任務定義

此任務分析本月支出增加的原因：

- 量的效應：買更多
- 價的效應：相似商品變貴

此任務只會針對重複購買、可比性較高的品項群進行，例如手搖飲、便利商店飲料、固定品牌食品等。若某品項只買過一次，或品名無法對齊，則不納入通膨拆解。

#### 技術方法

使用 Task 1 的 BERT embedding 作為商品語意表示，再用 BERTopic 與 HDBSCAN 進行微型商品群聚。

流程：

1. 對每筆品名與店家產生 BERT embedding
2. 使用 BERTopic 將相似品項形成主題
3. 使用 HDBSCAN 自動形成商品群
4. 過濾離群點與只出現一次的商品
5. 對每個商品群計算月均單價
6. 拆解支出增量中的量效應與價效應

HDBSCAN 的優點是可以自動把不穩定或只出現一次的樣本視為 noise，不強迫每個商品都進入某個群。這對發票資料很重要，因為真實品名經常不一致。

#### 拆解公式

對同一商品群，令：

- `P_0`：基期平均單價
- `Q_0`：基期購買數量
- `P_1`：當期平均單價
- `Q_1`：當期購買數量

總支出變化：

`Delta = P_1 * Q_1 - P_0 * Q_0`

可拆為：

- 價格效應：`(P_1 - P_0) * Q_1`
- 數量效應：`(Q_1 - Q_0) * P_0`

若兩者加總與實際增量有誤差，需回報誤差並檢查品項群是否不穩定。

---

## 六、風險與限制處理

### 6.1 資料量不足

風險：2,000 筆發票對 BERT fine-tuning 與時序模型來說不算大。

處理方式：

- 使用 pre-trained model，避免從零訓練
- 加入傳統 baseline，確認深度模型是否真的有改善
- 使用 early stopping、dropout、weight decay 降低 overfitting
- 對分類任務做 stratified split
- 對時序任務做 walk-forward validation
- 報告中清楚說明資料限制與泛化範圍

### 6.2 上游分類錯誤影響下游

風險：若 Task 1 分類錯誤，Task 2 的每日類別金額也會錯。

處理方式：

- 做兩組下游實驗：
  - 使用人工標註類別作為 oracle label
  - 使用 BERT 預測類別作為 real pipeline label
- 比較兩者差距，量化 error propagation
- 對低信心分類樣本建立 unknown 或人工覆核機制

### 6.3 發票資料不完整

風險：並非所有消費都有電子發票，例如現金小攤、轉帳、訂閱制服務。

處理方式：

- 將研究範圍明確定義為「可由電子發票觀察到的消費」
- 不宣稱能涵蓋使用者全部消費行為
- 在報告中把「發票覆蓋率」列為限制
- 若使用者提供補充記帳資料，可作為未來延伸

### 6.4 高價商品造成模型偏差

風險：手機、家電、精品等高價但正常的消費可能被誤判為異常。

處理方式：

- 加入高價商品標記
- 異常偵測同時看金額、類別與歷史習慣
- 對一次性高價商品另外分析，不直接視為情緒消費

---

## 七、實驗設計與評估

### 7.1 Task 1 分類實驗

實驗組合：

| 方法 | 輸入 | 目的 |
|---|---|---|
| Regex | 關鍵字 | rule-based 下限 |
| TF-IDF + Logistic Regression | 品名 + 店家 | 傳統 ML baseline |
| TF-IDF + MLP | 品名 + 店家 | 小型 neural baseline |
| BERT item-only | 品名 | 消融實驗 |
| BERT item-store | 品名 + 店家 | 驗證店家資訊 |
| BERT item-store-amount | 品名 + 店家 + 金額區間 | 完整模型 |

主要評估：

- Macro F1
- Weighted F1
- Confusion Matrix
- 空洞品名 subset F1

### 7.2 Task 2 預測實驗

實驗組合：

| 方法 | 特徵 | 目的 |
|---|---|---|
| Moving Average | 過去 7 天金額 | 最簡 baseline |
| ARIMA | 總金額時序 | 傳統時序 baseline |
| MLP | 30 天 window | 小型 neural baseline |
| LSTM | 金額序列 | 深度時序模型 |
| LSTM + time features | 金額 + 時間特徵 | 驗證時間特徵 |
| LSTM + time + semantic features | 金額 + 時間 + BERT embedding | 完整模型 |

主要評估：

- MAE
- RMSE
- sMAPE
- 對不同使用者分開報告結果

### 7.3 異常偵測評估

由於真實「情緒」標籤不存在，因此使用 proxy label：

- 當日總消費高於個人過去平均加 1.5 倍標準差
- 或高於個人歷史第 90 百分位
- 或出現個人少見類別的高額支出

評估指標：

- Precision
- Recall
- F1
- Top-k case study：列出異常分數最高的幾天，人工檢查是否合理

### 7.4 通膨拆解評估

評估重點不是預測準確率，而是拆解是否合理：

- 商品群是否語意一致
- 量效應 + 價效應是否接近總增量
- HDBSCAN 過濾 noise 後是否提升商品群品質
- 以案例方式呈現常購品項的單價變化

---

## 八、三人分工

### 成員 A：資料工程與 Task 1 分類模型

主要負責：

- 電子發票 CSV 匯入與欄位清洗
- 去識別化流程
- 品名與店家名稱正規化
- LLM 預標註流程與標註準則
- BERT 分類模型 fine-tuning
- TF-IDF、Regex、MLP baseline
- Task 1 評估與 confusion matrix 分析

交付成果：

- 清洗後資料集
- 標註資料集
- BERT 分類模型
- Task 1 實驗結果表

### 成員 B：時序預測與異常偵測

主要負責：

- 將發票明細彙整為每日消費矩陣
- 時間特徵工程
- Moving Average、ARIMA、MLP baseline
- LSTM 消費金額預測模型
- LSTM Autoencoder 異常偵測
- 補 0 與 mask 對模型影響的分析
- Task 2 Layer 1 與 Layer 2 評估

交付成果：

- 每日消費時序資料
- 預測模型與 baseline
- 異常偵測模型
- 預測誤差與異常案例分析

### 成員 C：語意分群、通膨拆解與整合報告

主要負責：

- 使用 Task 1 embedding 進行商品語意分群
- BERTopic 與 HDBSCAN 實驗
- 重複購買商品群篩選
- 單價時序追蹤
- 量效應與價效應拆解
- 整體 pipeline 整合
- 最終報告、簡報圖表與系統架構圖

交付成果：

- 商品分群結果
- 通膨拆解案例
- 完整系統架構圖
- 最終報告與簡報整合

### 共同工作

三位成員共同負責：

- 人工審核標註資料
- 討論分類準則
- 檢查模型輸出是否符合常識
- 撰寫限制與未來改進
- 期末口頭報告

---

## 九、時程規劃

| 週次    | 工作項目                               | 主要負責 |
| ----- | ---------------------------------- | ---- |
| 第 1 週 | 資料收集、去識別化、欄位確認                     | A、B  |
| 第 2 週 | 標註準則、LLM 預標註、人工審核                  | A、全員 |
| 第 3 週 | Task 1 baseline 與 BERT fine-tuning | A    |
| 第 4 週 | 每日消費矩陣、時序 baseline、LSTM 預測         | B    |
| 第 5 週 | LSTM Autoencoder、異常偵測案例            | B    |
| 第 6 週 | BERTopic/HDBSCAN、通膨拆解案例            | C    |
| 第 7 週 | 實驗整合、圖表、報告與簡報                      | 全員   |

若進度不足，優先完成：

1. Task 1 BERT 分類與 baseline 比較
2. Task 2 Layer 1 LSTM 預測與 baseline 比較
3. Layer 2 異常偵測案例
4. Layer 3 通膨拆解案例

---

## 十、預期成果

本專案預期完成以下成果：

- 一套可處理台灣電子發票 CSV 的資料前處理流程
- 一個能結合品名、店家與金額資訊的 BERT 消費分類模型
- 一組傳統 ML 與 rule-based baseline，用於驗證 BERT 的必要性
- 一個以每日消費矩陣為輸入的短期消費預測模型
- 一個基於 LSTM Autoencoder 的異常消費偵測方法
- 一個使用 BERT embedding、BERTopic、HDBSCAN 的商品語意分群方法
- 個人消費增量的量效應與價效應拆解案例
- 對資料量、標註品質、發票覆蓋率與模型泛化能力的完整討論

最終報告將強調本專案的價值不是「準確預測每一筆消費」，而是展示如何把真實電子發票資料轉換成可分類、可預測、可診斷的個人消費行為分析系統。

